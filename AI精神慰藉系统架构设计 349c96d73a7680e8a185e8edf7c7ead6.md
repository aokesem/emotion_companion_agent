# AI精神慰藉系统架构设计

# 设计原则

我们希望为独居老人打造一个情感对话Agent。经过调查，我们认为现有技术总体是为"完成某个任务"而设计的（因为底层训练方式如此）。而我们的核心需求对目标人群进行存在性确认和相应的精神关怀。这是底层设计思路的不同。

目标人群所需要的对话助手画像大致为：带有不同稳定人格但能自适应对话风格、响应式收集信息建立长期个性化、满足关系性需求并缓解用户对话孤岛的长期个性化AI慰藉助手

现有产品总的来说有以下问题

- 1.模型训练目标的结构性偏差
- 2.情感记忆的颗粒度停留于事实层面
- 3.情感识别停留于显式分类，对于隐性表达不够理解
- 4.Agent定位为服务者而忽视了目标人群的“被需要感”
- 5.缺乏响应式间接引导机制，现有方案呈现完全被动或清单式提问的两级，缺少自然对话延申
- 6.总体只顾指标，没有从用户视点考虑互惠性

而我们的解决方案如下

- 1.训练目标的结构性偏差
    - 监督微调：构造一批"情感优先"的对话示例，让模型学会模仿这种模式
    - 强化学习：训练奖励模型判断一个回复是"情感共鸣"还是"方案导向"，再不断打分优化
- 2.情感记忆的颗粒度
    - 标准RAG不足，除向量存储外，增加时间-情感-事实 索引
    不仅记用户说喜欢xx，同时记"那天他提起xx时带着对什么样的情绪，之后几次对话未提及，间隔两周后再次提起时语气转为怀旧"。
    这需要显式的情感状态机和遗忘机制。
- 3.隐喻/隐性情感识别
    - 思维链：强制模型进行潜台词推断再进行回应
    - 小模型微调：寻找一个包含隐喻-真实情感映射的数据集上训练专门的情感解码器，在输入对话时先行判断
- 4.被需要感/示弱机制
    - 提示词工程：使得AI具有特定的人设（如谦虚的晚辈/熟识的老友），更加满足用户对话需求
    - 自适应对话策略：对话中先判断用户话术，针对性的进行策略控制（如让AI不表示夸赞，而是询问细节）
- 5.响应式间接引导
    - 用户情绪建模：如设计一个动态更新的连续值，当用户主动提及某事，更开发的态度，那么情绪值上升；若用户有意避开则下降；若用户多次提及但情绪是抱怨或者其他，则情绪值不变
    这将决定Agent的引导力度
    - 话题辨别与过渡：
    在对话中检测用户的情感色彩词和个人信息，用来制作用户画像；
    当检测到新信号时，用软过渡语言（"您提到……让我想起……"、"说到这个……"）而非直接提问来延伸
    - 网状记忆结构：
    维护一个松散的话题图，节点是用户曾经提及的人、事、情绪
    根据用户当前的情绪开放度，调取一个与当前节点相关联的事件节点以及其附带的人、事、情绪

我们决定先从构建Agent开始，先把架构构建出来，再进行模型调优和策略方法的调整。以下是目前设计的数据存储层、系统处理流程和技术选型。

# 数据存储层

### **2.1 用户画像（结构化，被动积累）**

```json
{
  "basic": {
    "age": null,
    "gender": null,
    "occupation": null,
    "health_notes": null
  },
  "preferences": {
    "hobbies": [],
    "topics_of_interest": [],
    "avoided_topics": []
  },
  "interaction_style": {
    "language_habit": null,
    "response_length_preference": null,
    "openness_baseline": 0.3
  }
}
```

**更新方式**：记忆写入模块（模块⑥）每轮对话结束后被动提取并更新，不主动询问。 **调用方**：模块④策略控制器 + 模块⑤生成Agent。

---

### **2.2 知识图谱（图结构，情感叙事记忆）**

每个节点结构：

```json
{
  "id": "unique_id",
  "type": "人物/地点/事件/情感",
  "label": "姐姐",
  "narrative_time": "30年前",
  "system_time": "2024-03-15",
  "retrieval_weight": 0.85,
  "emotions": [
    {"label": "悲伤", "intensity": "高", "context": "姐姐去世"},
    {"label": "思念", "intensity": "中", "context": "过年时提及"}
  ],
  "relations": ["父亲(兄妹关系)", "老家(地点关联)"]
}
```

**遗忘规则**（低优先级，后期补充）：

- 超过30天未访问 → **`retrieval_weight × 0.9`**
- 同一事件新情感与旧情感相反 → 更新主情感标签，旧标签保留为历史

**检索方式**：按**`retrieval_weight`**排序取前N名，结合当前话题语义相似度联合排序。

---

### **2.3 对话历史（向量存储，RAG检索）**

每条记录：

```json
{
  "turn_id": "xxx",
  "timestamp": "2024-03-15T20:30:00",
  "user_text": "原始文本",
  "emotion_label": "思念",
  "emotion_intensity": "中",
  "self_disclosure_level": "L3",
  "embedding": [...]
}
```

---

### **2.4 AI角色档案（静态核心 + 动态兴趣种子）**

```json
{
  "static": {
    "persona": "谦逊好奇的年轻人",
    "tone": "口语化、节奏慢、善用停顿",
    "forbidden_behaviors": ["给建议", "说教", "结构化长篇回复"]
  },
  "dynamic": {
    "current_interest_seed": "[根据用户画像自动填入，如'对园艺一窍不通']",
    "current_weakness_topic": "[用户专长领域，AI在此示弱]"
  }
}
```

---

### **2.5 心理技巧库（策略插件）**

```json
[
  {
    "id": "reminiscence",
    "name": "怀旧疗法",
    "trigger": "用户提及过去经历 AND 开放度 > 0.5",
    "prompt_template": "主动邀请叙述，用'您那时候是怎么……'引导"
  },
  {
    "id": "active_listening",
    "name": "积极倾听",
    "trigger": "开放度 < 0.3 OR 用户处于防御状态",
    "prompt_template": "反映情感，不给建议，用'听起来您当时……'回应"
  },
  {
    "id": "logotherapy",
    "name": "意义疗法",
    "trigger": "情感类型 == 存在性焦虑 AND 情感强度 == 高",
    "prompt_template": "引导发现价值，用'您觉得那段经历让您懂得了……'引导"
  }
]
```

# 系统处理流程

### **模块① 隐性情感解码器**

**类型**：单次LLM调用（非Agent） **职责**：Gap 3 — 推断潜台词，识别隐喻情感 **输入**：用户原始文本 + 最近3轮对话上下文 **输出**：

```json
{
  "surface_content": "今天天气真烂",
  "inferred_emotion": "孤独/低落",
  "confidence": "中",
  "intensity": "低",
  "recommended_tone": "试探性共情"
}
```

**关键设计**：置信度低时输出**`recommended_tone = "试探性共情"`**，避免过度解读导致用户逃避。

---

### **模块② 情绪开放度追踪器**

**类型**：规则计算函数（无需LLM） **职责**：Gap 5 — 维护并更新连续值状态 **输入**：模块①输出 + 上轮开放度值 **更新规则**：

| **信号** | **权重变化** |
| --- | --- |
| 用户主动叙述个人经历（L3级表露） | +0.2 |
| 用户"我其实……"等自我暴露前缀 | +0.3 |
| 用户回复简短或转移话题 | -0.1 |
| 用户"哈哈没事"等轻笑回避 | -0.1 |
| 用户重复抱怨同一件事 | ±0（潜在需求信号，不升不降） |

**输出**：更新后的开放度值（0到1），同时标记是否出现"情绪转折点"（如从抱怨突然到"算了不说了"）。

---

### **模块③ 记忆检索模块**

**类型**：数据库查询函数（无需LLM） **职责**：Gap 2 — 从长期记忆中调取候选上下文 **输入**：用户原始文本 + 模块①的情感标签 **执行**：

- **RAG检索**：在对话历史向量库中检索语义相似的历史记录（Top 3）
- **图谱查询**：从知识图谱中检索当前话题相关节点及其情感锚点（按**`retrieval_weight`**排序取Top 3）

**输出**：候选记忆列表（注意：**只是候选，是否使用由模块④决定**） **关键设计**：记忆检索结果不直接注入回复，必须经过模块④的"记忆引入时机"判断。

---

### **模块④ 对话策略控制器**

**类型**：规则逻辑 + 轻量LLM判断（混合） **职责**：Gap 4 + Gap 5 — 决定本轮行为模式 **输入**：模块①②③的所有输出 + 用户画像 + AI角色档案 **决策逻辑**：

`步骤一：记忆引入时机判断
  IF 开放度 < 0.5:
      → 禁止引入长期记忆（只用当前轮上下文）
  ELIF 开放度 >= 0.5 AND 候选记忆与当前话题相关度高:
      → 允许以"我记得你说过"方式软引入

步骤二：疗法选择
  IF 用户提及过去经历 AND 开放度 > 0.5:
      → 加载怀旧疗法
  ELIF 开放度 < 0.3 OR 置信度低:
      → 加载积极倾听
  ELIF 情感 == 存在性焦虑 AND 强度 == 高:
      → 加载意义疗法
  ELSE:
      → 默认策略

步骤三：角色姿态选择
  IF 开放度 < 0.3:
      → 深度示弱（请教细节，晚辈姿态）
  ELIF 0.3 <= 开放度 < 0.7:
      → 软过渡延伸（关联记忆，老友姿态）
  ELSE:
      → 陪伴倾听（知己姿态）

步骤四：转折点处理
  IF 检测到情绪转折点（如突然沉默/算了不说了）:
      → 强制切换为积极倾听策略，暂停记忆引入`

**输出**：策略指令包（疗法模板 + 角色姿态 + 记忆引入许可 + 推荐语气）

---

### **模块⑤ 对话生成Agent**

**类型**：真正的Agent（核心LLM调用） **职责**：整合所有上下文生成回复 **输入**：策略指令包 + 候选记忆（如许可引入）+ 用户原始文本 + AI角色档案 **系统提示词结构**：

`[静态人设] + [动态兴趣种子] + [本轮疗法模板] + [角色姿态指令]
+ [禁止行为列表] + [软过渡语言示例] + [记忆引入指令（如许可）]`

**输出**：自然语言回复

---

### **模块⑥ 记忆写入模块**

**类型**：数据库写入函数 + 轻量LLM提取 **职责**：Gap 2 — 将本轮信息持久化 **执行**：

1. **情感提取**：复用模块①的结果，补充**`intensity`**三档判断（低/中/高）
2. **实体提取**：用轻量Prompt从用户发言中提取人名/地点/事件词
3. **向量存储**：将本轮对话存入RAG向量库（带情感标签）
4. **图谱更新**：新节点建立，已有节点更新情感列表和**`retrieval_weight`**
5. **用户画像更新**：如本轮出现结构化信息（年龄/职业/爱好）则更新对应字段

# 技术选型

| **组件** | **选型** |
| --- | --- |
| Agent编排框架 | LangGraph |
| 向量数据库（RAG） | Supabase + pgvector |
| 图数据库 | Neo4j |
| 用户画像存储 | Supabase（PostgreSQL） |
| 心理技巧库 | JSON文件 / Supabase表 |